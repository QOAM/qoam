@using QOAM.Core
@model QOAM.Core.Price

@if (Model == null)
{
    
}
else if (Model.FeeType == FeeType.NoFee)
{
    @QOAM.Website.Resources.Strings.NoFee
}
else if (Model.FeeType == FeeType.Absent)
{
    @QOAM.Website.Resources.Strings.Absent
}
else if (Model.Amount.HasValue && Model.Amount.Value == 0)
{
    @QOAM.Website.Resources.Strings.Free
}
else if (Model.FeeType == FeeType.Page)
{
    @Html.DisplayFor(m => m.Currency)
    @Model.Amount.ToString()
    @QOAM.Website.Resources.Strings.PricePerPage
}
else
{
    @Html.DisplayFor(m => m.Currency)
    @Model.Amount.ToString()
    @QOAM.Website.Resources.Strings.PricePerArticle
}
